# docker compose 环境变量（复制为 .env 后按需修改）
# 生产务必修改 JWT_SECRET，并设置 FERNET_KEY（见 backend/.env.example 生成说明）

# 后端密钥
JWT_SECRET=change-me-in-production-please
FERNET_KEY=

# Redis（可选）：留空 = 走 PG 回源（KV 缓存 / 章节锁）
REDIS_URL=
KV_CACHE_ENABLED=true
